auto lo
iface lo inet loopback

# Wired interface
auto eth0
iface eth0 inet dhcp

# Wireless interface
auto wlan0
iface wlan0 inet dhcp
    pre-up wpa_supplicant -B -i wlan0 -c /etc/wpa_supplicant/wpa_supplicant-wlan0.conf -D nl80211,wext
    post-down killall -q wpa_supplicant || true
